# 穿搭对齐待办（等衣橱分类树做完再改）

衣橱已按 [v3-衣橱分类树.md](v3-衣橱分类树.md)、[v4-分类细化.md](v4-分类细化.md) 做成可改名、排序、增删、两层小分类，以及删除回上级、详情页加子分类、树状移动、同级不重名、每级独立封面。穿搭还在用 [`lib/core/catalogs.dart`](../lib/core/catalogs.dart) 里写死的场合列表。以后改穿搭时按下面做，两边交互保持一致。

## 首页

- [ ] 去掉穿搭首页右上角排序；搜索结果仍按添加时间新→旧
- [ ] 换成「分类管理」入口（对应衣橱 `/wardrobe/categories`）

## 分类数据

- [ ] 场合分类写入同一张 `categories` 表，`kind = outfit`
- [ ] 迁移时按现有 id 种子：无分类、工作、休闲、派对、运动、度假
- [ ] 「无分类」可改名、排序，不能删
- [ ] 可新增大分类；大分类下最多两层小分类
- [ ] 删除：子树一起删；有上级则穿搭回到**直接上一级**，删大分类才回无分类；确认弹窗写出实际去向
- [ ] 同一层名称不能重复；不同父分类下的子分类可以同名；新增、改名都要拦，提示「同一层已有「某某」」
- [ ] 已存在的同名分类不做自动改名

## 浏览

- [ ] 点分类一律进详情页（空分类也不跳到添加）
- [ ] 详情页：子分类卡片 + 直接挂在这一层的穿搭；封面和件数按子树
- [ ] 还能再往下分一层时，详情页可「添加子分类」；创建时可勾选当前层已有穿搭一并移入
- [ ] 分类管理页同样能改名、同级上移/下移、添加子分类、删除
- [ ] 返回到父分类；大分类回穿搭首页

## 封面

- [ ] 每个分类（含小分类）独立封面，规则与衣橱相同：自定义仍在子树内则用它，否则该子树 `createdAt` 最新一件
- [ ] 同一件可同时当祖先分类和自己所属分类的封面，不能当旁系分类封面
- [ ] 穿搭详情里按路径勾选「设为封面」；分类页可「恢复默认封面」

## 表单与详情

- [ ] 编辑页用完整路径选分类（缩进或 `工作 / …`）
- [ ] 详情显示完整路径
- [ ] 详情顶栏「移动」，弹出整棵分类树选目标；不要把移入/移出按钮叠在封面上
- [ ] 不要抄衣橱的尺码测量、「物件 / 尺寸」模板

## 建议复用

- [`lib/core/category_tree.dart`](../lib/core/category_tree.dart) 的深度、路径、子树、同级重名检查、删除回上级
- [`lib/data/category_repository.dart`](../lib/data/category_repository.dart) 按 `kind` 操作，不要再写一套 SQL
- [`lib/data/item_repository.dart`](../lib/data/item_repository.dart) 的 `moveToCategory` 思路（穿搭仓库同样加批量改 `categoryId`）
- [`lib/features/wardrobe/category_item_dialogs.dart`](../lib/features/wardrobe/category_item_dialogs.dart) 的添加子分类勾选、树状移动弹窗
- 分类管理页可抽成共用组件，衣橱/穿搭只传 `CategoryKind`
